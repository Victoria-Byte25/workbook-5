package com.pluralsight;

public class Moped extends Vehicle {
    // Constructor that passes values to the Vehicle parent
    public Moped(String color, int passengers, int cargo, int fuel) {
        super(color, passengers, cargo, fuel);
    }
}
